When this plugin is activated, whenever a block is broken, it drops a random amount of a random material.
In order to activate it, type /randomdrops true. In order to deactivate it, type /randomdrops false.
